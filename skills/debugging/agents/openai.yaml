interface:
  display_name: "Debugging"
  short_description: "Reproduce failures and verify their root causes"
  default_prompt: "Use $debugging to reproduce this failure, isolate its root cause, and verify the smallest appropriate fix."
