# Sharing Backend Work Local Background

This file is a template for local, non-portable Slack routing context. Copy it to
`skills/sharing-backend-work/background.local.md`; the copied file is ignored by git.

`SKILL.md`와 `references/case-templates.md`는 채널을 역할 이름으로만 부릅니다.
실제 채널 이름은 이 파일에서 해석합니다.

## Channels

- 전사 채널: `{#앱·웹·백엔드가 함께 읽는 채널}`
- 백엔드 채널: `{#동료 백엔드와 리드가 읽는 채널}`
- 배포 기록 채널: `{#배포 시각 기록만 남기는 채널}`
- 자동 알림 채널: `{#시스템 알림}`, `{#배포 알림}`, `{#인프라 알림}` — 사람이 공유 글을 쓰지 않습니다.

## Lead

- 리드 멘션: `{@리드}`
