interface:
  display_name: "IssueOps Implement"
  short_description: "Run the generation-fenced IssueOps implement phase"
  default_prompt: "Use $issueops-implement only for the IssueOps implement phase after design, compatibility, and devil's-advocate approval. Verify the execution lease, native actor, and canonical worktree before any mutation, implement with a focused failing test first, delegate bounded work through child cycles with accept/reject/drop verdicts only, run a real planner-grade adversarial review before recording it, recover only through returned next_command chains, and exit by moving the cycle to ai-slop-clean. Route publication to $issueops-create-pr and lifecycle routing to $issueops."
