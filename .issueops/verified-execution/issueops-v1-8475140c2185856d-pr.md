## 요약

- reviewed #447 Run-scoped Orca inventory 의존성을 #449 ancestry에 반영했습니다.
- collector가 하나의 Run inventory snapshot을 재사용하고 Git/Orca inventory 수집을 병렬화합니다.

## 변경 사항

- Run snapshot의 runtime·Run ID를 검증한 뒤 all/dispatched/gate projection에 동일 snapshot을 전달합니다.
- 기존 dispatched cross-check와 오류의 결정적 순서를 보존합니다.
- snapshot 공유, 병렬 수집, runtime mismatch fail-closed 동작을 회귀 테스트로 고정했습니다.

## 검증

- `go test ./internal/adapter/orca ./internal/adapter/operationalhealth ./internal/architecture -count=1`
- `go test -race ./internal/adapter/orca ./internal/adapter/operationalhealth -count=1`
- `go test -v ./internal/adapter/operationalhealth -count=1`
- `go test -race ./internal/adapter/operationalhealth -count=1`
- `git diff --check`

## 범위

- #447 dependency 파일은 reviewed commit으로만 반영했고 수동으로 수정하지 않았습니다.
