# CLAUDE.md

Claude Code에서 이 저장소를 열면 먼저 `AGENTS.md`를 읽고 동일한 규칙을 따른다.

- 공용 하네스 결정과 작업 계약: `AGENTS.md`
- 상세 문서: `agent_docs/`
- Claude Code native skill: `.claude/skills/atomic-commit-push` 또는 `~/.claude/skills/atomic-commit-push`
- Claude Code MCP: 프로젝트 루트 `.mcp.json`의 `agent-harness` 서버가 `bin/harness mcp`를 실행한다.
- 사용법은 `agent_docs/USAGE.md`를 따른다.
