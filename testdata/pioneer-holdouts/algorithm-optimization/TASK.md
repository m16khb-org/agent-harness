# DIJKSTRA-H1

`startup.py` is reported as slow. `profile.txt` is a profiler run of one
`startup()`. Decide whether and what to optimize. Justify the decision from the
profile, and if you recommend a change, state the input size at which it would
actually matter. Do not optimize code that the profile shows is off the hot
path.
