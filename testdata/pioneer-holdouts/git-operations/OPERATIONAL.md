# TORVALDS-O1
Plan a safe interactive rebase of twelve local commits with two conflict-prone changes and an upstream branch that must not be rewritten. Include backup refs, atomic intent grouping, conflict recovery, per-commit verification, and push constraints.
