# HOPPER-H1

`bash run.sh` exits non-zero. Diagnose the failure from first principles,
apply the minimal fix, and verify the script then exits 0. Do not hard-code
around the symptom or stub the check out.
