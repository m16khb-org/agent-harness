{
  "id": "pioneer-dijkstra",
  "title": "Algorithmic hot path needs complexity optimization",
  "user_prompt": "주문 매칭 루프가 N=10000에서 4초나 걸려. O(n^2) 같은데 최적화해줘",
  "repo_context": "Go service with a nested-loop order matcher in internal/match. A CPU profile exists showing the matcher dominates runtime. The dijkstra skill must drive complexity analysis with scaling evidence.",
  "pioneer_skill_target": "dijkstra",
  "expected_issue": ["states the measured hot path", "defines acceptance criteria with a complexity target", "is written in Korean", "references issue/pr guideline"],
  "expected_plan": ["profiles before optimizing", "states target complexity class", "includes verification"],
  "expected_tasks": ["bounded ownership"],
  "expected_tdd": ["failing benchmark or test before implementation"],
  "expected_subagents": ["verifies pwd branch and HEAD before work"],
  "expected_pr": ["includes before/after benchmark evidence", "links issue", "is written in Korean", "references issue/pr guideline"],
  "expected_pioneer_artifact": ["Hot path", "Complexity", "Scaling evidence", "Correctness invariant", "Before/after measurement"],
  "critical_failures": ["skips pioneer method", "works in source repo", "worker starts without context check", "unbounded code-reviewer review", "issue or pr/mr not written in korean", "missing issue/pr guideline reference", "excessive emoji in issue or pr/mr"]
}
