name: CI

# Mirrors the repo's already-deterministic local verification gate (AGENTS.md §9)
# as a manual workflow while the Linux runner-only test failures are investigated.
# The self-verify gate runs with --judge none (LLM eval off by default) and a
# pinned --seed, so this is pure wiring of existing commands: no live-skill
# invocation and no model is required in CI. See
# .agent-harness/plans/harness-quality-program-v2-externally-benchmarked.md (A1).

# Re-enable the push / pull_request triggers below once the Linux-runner-only
# test failures are resolved; until then CI stays manual-dispatch so a red
# Linux run does not block every push.
on:
  workflow_dispatch:
  # push:
  #   branches: [main]
  # pull_request:

# Cancel superseded manual runs on the same ref to save runner minutes.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
          cache: true

      - name: Format check (gofmt)
        # gofmt does not understand the `./...` package pattern, so check the
        # tracked Go files explicitly (this also skips vendored / worktree copies).
        run: |
          unformatted="$(gofmt -l $(git ls-files '*.go'))"
          if [ -n "$unformatted" ]; then
            echo "These files are not gofmt-clean:"; echo "$unformatted"; exit 1
          fi

      - name: Vet
        run: go vet ./...

      - name: Build
        run: go build -o bin/agent-harness ./cmd/harness

      - name: Test (catches golden drift + all unit/contract regressions)
        run: go test ./... -count=1

      - name: Deterministic self-verify gate (seed-pinned, --judge none)
        run: ./bin/agent-harness self-verify --seed=100 --target-score=95 --json
